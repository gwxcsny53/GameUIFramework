import { JsonAsset, Prefab } from "cc";

export const ResourceGroups = {
    BattleCommon: [
        { path: "json/skill", type: JsonAsset },
        { path: "prefabs/battle/enemy", type: Prefab },
    ],
};
